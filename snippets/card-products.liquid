{% assign hidden_product_id = 15706174095694 %}
{% unless card_product.id == hidden_product_id %}
<div>
  <div class="product-box coll-page">
    <a href="{{ card_product.url }}">
      <div class="box-img">
        <img
          srcset="
            {%- if card_product.featured_media.width >= 165 -%}{{ card_product.featured_media | image_url: width: 165 }} 165w,{%- endif -%}
            {%- if card_product.featured_media.width >= 360 -%}{{ card_product.featured_media | image_url: width: 360 }} 360w,{%- endif -%}
            {%- if card_product.featured_media.width >= 533 -%}{{ card_product.featured_media | image_url: width: 533 }} 533w,{%- endif -%}
            {%- if card_product.featured_media.width >= 720 -%}{{ card_product.featured_media | image_url: width: 720 }} 720w,{%- endif -%}
            {%- if card_product.featured_media.width >= 940 -%}{{ card_product.featured_media | image_url: width: 940 }} 940w,{%- endif -%}
            {%- if card_product.featured_media.width >= 1066 -%}{{ card_product.featured_media | image_url: width: 1066 }} 1066w,{%- endif -%}
            {{ card_product.featured_media | image_url }} {{ card_product.featured_media.width }}w
          "
          src="{{ card_product.featured_media | image_url: width: 533 }}"
          sizes="(min-width: {{ settings.page_width }}px) {{ settings.page_width | minus: 130 | divided_by: 4 }}px, (min-width: 990px) calc((100vw - 130px) / 4), (min-width: 750px) calc((100vw - 120px) / 3), calc((100vw - 35px) / 2)"
          alt="{{ card_product.title | escape }}"
          class="motion-reduce"
          loading="lazy"
          width="{{ card_product.featured_media.width }}"
          height="{{ card_product.featured_media.height }}"
        >
        <div class="main-tags">
          {%- for coll in card_product.collections -%}
            {% if coll.title == settings.collectionss.title %}
              
              {% comment %} NA DE BLACKFRIDAY AANZETTEN {% endcomment %}
              {% comment %} <div class="collname" style="color: {{ settings.tag_color }}; background: {{ settings.tag_background }};">
                <span class="super" style="color: {{ settings.tag_color }};">{{ settings.tag_title }}</span>
                <span class="sale" style="color: {{ settings.tag_color2 }};">{{ settings.tag_title2 }}</span>
              </div> {% endcomment %}
            
              <div class="notification-bar" style="background:none!important; padding: 0px!important;border-radius: 10px!important;">
                <div class="notification-block">
                    <h6 style="padding: 4px 10px !important; width: 100%; border-radius: 5px;">
                        <b>SUPER</b>
                        <span>SALE</span>
                    </h6>
            </div>
          </div>
        {% endif %}
          {% endfor %}
          {%- if card_product.compare_at_price > card_product.price -%}
            {% capture discount %}{{ card_product.compare_at_price_max | minus:card_product.price | money }}€{% endcapture %}
            <label>€{{ discount | remove: ',00€' | remove: '€' }} korting</label>
          {% endif %}
        </div>
      </div>
      <!-- FIX #1: Added 'data-stockinfo' so the JavaScript can find the delivery text. -->
 

 
      <div class="description" data-stockinfo="{{ card_product.metafields.arena.stockinfo | escape }}">
        <h3>{{ card_product.title }}</h3>
        {%- if settings.show_variant_count -%}
          {%- assign variant_count = 0 -%}
          {%- if card_product.metafields.custom.subproducts != blank -%}
            {%- assign subproduct_urls = card_product.metafields.custom.subproducts.value | compact -%}
            {%- assign variant_count = subproduct_urls.size | plus: 1 -%}
            {%- if variant_count > 1 -%}
              <span class="variant-count">{{ variant_count }} varianten</span>
            {%- endif -%}
          {%- else -%}
            {%- assign variant_count = card_product.variants.size -%}
            {%- if variant_count > 1 -%}
              <span class="variant-count">{{ variant_count }} varianten</span>
            {%- endif -%}
          {%- endif -%}
        {%- endif -%}
        {%- assign current_variant = card_product.selected_or_first_available_variant -%}
        <div class="product-card-stock-info">
          {% if current_variant.inventory_quantity > 0 %}
            <h5 class="stockss show-stock-desktop show-stock-tablet show-stock-mobile">Op voorraad</h5>
          {% elsif card_product.metafields.arena.stockinfo != blank %}
            <h5 class="weekss show-delivery-weeks-desktop show-delivery-weeks-tablet show-delivery-weeks-mobile">{{ card_product.metafields.arena.stockinfo }}</h5>
          {% else %}
            <h5 class="weekss show-delivery-weeks-desktop show-delivery-weeks-tablet show-delivery-weeks-mobile">Niet op voorraad</h5>
          {% endif %}
        </div>


        {%- if card_product.compare_at_price > card_product.price -%}
            <p>
              Adviesprijs <span class="sale-line-as-before">{{ card_product.compare_at_price | money }} </span>
            </p>
         
 <h6>{{ card_product.price | money }}</h6>

        {% endif %}
       
      </div>
    </a>
  </div>
</div>

<style>
  .variant-count {
    display: block;
    font-size: {{ settings.variant_count_font_size }}px;
    font-weight: {{ settings.variant_count_font_weight }};
    color: rgba(var(--color-foreground), 0.7);
    margin-bottom: 0.5rem;
  }
</style>
{% endunless %}
